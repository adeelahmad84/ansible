---

- name: Generate CDN configuration files
  hosts: localhost
  gather_facts: no
  connection: local

  vars_files:
    - roles/common/vars/sites/syost.yml
    - roles/bbpe/vars/syost.yml

  roles:
    - bbpe
