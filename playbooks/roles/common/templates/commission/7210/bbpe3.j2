exit all
configure
    router
        policy-options
            begin
            prefix-list "PL-POP-CDN-LOOPBACK"
                prefix {{ management | ipaddr('0') }} exact
            exit
	    commit
	exit
	static-route {{ management | ipaddr('0') }} next-hop {{ bbpe.3.ipv4 | ipaddr('1')|replace("/31"," ") }} tag 20100
exit all