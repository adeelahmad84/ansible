configure terminal
 default interface Bundle-Ether{{ old_last_octet|int + 256 }}
 default router isis interface Bundle-Ether{{ old_last_octet|int + 256 }}
 default rsvp interface Bundle-Ether{{ old_last_octet|int + 256 }}
 default mpls ldp interface Bundle-Ether{{ old_last_octet|int + 256 }}
 default mpls traffic-eng interface Bundle-Ether{{ old_last_octet|int + 256 }}
mpls ldp
 neighbor
  no {{ old_system_address }}:0 password
 !
 address-family ipv4
  label
   remote
    accept
     no from {{ old_system_address }}:0 for loopback-only
 !
commit
