---

- name: Generate CDN configuration files
  hosts: localhost
  gather_facts: no
  connection: local

  vars_files:
    - roles/common/vars/sites/ensts.yml
    - roles/bbpe/vars/ensts.yml

  roles:
    - bbpe
