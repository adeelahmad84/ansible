---

- name: Generate CDN configuration files
  hosts: localhost
  gather_facts: no
  connection: local

  vars_files:
    - roles/common/vars/sites/sabir.yml
    - roles/bbpe/vars/sabir.yml

  roles:
    - bbpe
